shadow = false;
// shadow-radius	= 20;
// shadow-offset-x	= -12;
// shadow-offset-y	= -12;
// shadow-opacity = 0.80;

// corner-radius = 10.0;

// requires backend = "glx";
blur-background = false;
blur-method = "dual_kawase";
blur-strength = 4;

fading = false;
// fade-delta = 7.5;
// fade-in-step = 0.056;
// fade-out-step = 0.06;

// vsync = true;
// dbe	= false;
// backend = "glx";

// shadow-exclude = [
//   "name ='polybar' || name ='Looking Glass (client)' || name = 'tray'"
// ]
// rounded-corners-exclude = [
//   "name ='polybar' || name ='Looking Glass (client)' || name = 'tray'"
// ];

inactive-dim = 0.50;

// opacity-rule = [
//   "100:class_g = 'Rofi'",
//   "100:class_g = 'i3lock'",
//   "70:!focused",
//   "99:class_g = 'st-256color'",
// ]

focus-exclude = [
  "class_g = 'dwm'",
  "class_g = 'Rofi'",
  "class_g = 'dmenu'",
  "class_g = 'rhkd-whichkey'",
]
