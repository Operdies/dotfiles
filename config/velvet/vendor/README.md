# vendor

Some useful lua libraries I didn't write. The better solution would probably be
to create an official velvet plugin system and actually vendor useful modules
that don't fit in the velvet distribution.
